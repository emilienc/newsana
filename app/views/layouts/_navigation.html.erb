
<%= link_to raw("<strong><i class='icon-large icon-home'></i> #{t('nav.home')}</strong>"), user_signed_in? ? user_path(current_user) : root_path , :class => 'brand' %>

<ul class="nav">
  <% if user_signed_in? %>

 
    <li class = "dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%=raw("<i class='icon-large icon-user'></i> #{t('nav.infos')}")%><b class="caret"></b></a>  
         <ul class="dropdown-menu">  
           <% unless current_user.guest? %>
           <li><%= link_to t('nav.account'), edit_user_registration_path %></li>
           <%else%>
           <li><%= link_to "Devenir Membre", new_member_path %></li>
           <%end%>
           <li class="divider"></li>  
            <% if current_user.profile.nil? %>
                <li><%= link_to t('nav.profil'), new_user_profile_path(current_user.id) %></li>
            <%else%>
                <li><%= link_to t('nav.profil'), edit_user_profile_path(current_user.id,current_user.profile.id) %></li>
            <%end%>
           
         </ul>  
    </li>  
    <li class = "dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%=raw("<i class='icon-large icon-signal'></i> #{t('nav.weight.title')}")%><b class="caret"></b></a>  
         <ul class="dropdown-menu">  
            <li><%= link_to t('nav.weight.add'), new_pesee_path %></li>      
            <li><%= link_to t('nav.weight.see'), pesees_path %></li>    
            <% if current_user.target.nil? %>
                <li><%= link_to t('nav.weight.target'), new_user_target_path(current_user) %></li>
            <%else%>
                <li><%= link_to t('nav.weight.target'), edit_user_target_path(current_user,current_user.target) %></li>
            <%end%>
         </ul>  
    </li>  
     <li class = "dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%=raw("<i class='icon-large icon-glass'></i> #{t('nav.food.title')}")%><b class="caret"></b></a>  
         <ul class="dropdown-menu">  
            <li><%= link_to t('nav.food.add'), new_repa_path %></li>      
            <li><%= link_to t('nav.food.see'), repas_path %></li>    
         </ul>  
    </li>  
    <li class = "dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%=raw("<i class='icon-large icon-road'></i> #{t('nav.exercise.title')}")%><b class="caret"></b></a>  
         <ul class="dropdown-menu">  
            <li><%= link_to t('nav.exercise.add'), new_activite_path %></li>      
            <li><%= link_to t('nav.exercise.see'), activites_path %></li>    
         </ul>  
    </li>

    <li class = "dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%=raw("<i class='icon-large icon-question-sign'></i> #{t('nav.help.title')}")%><b class="caret"></b></a>  
         <ul class="dropdown-menu">  
            <li><%= link_to t('nav.help.help'), help_path %></li>
            <li><%= link_to t('nav.help.questions'), questions_path %></li>
            <li><%= link_to t('nav.help.about'), about_path %></li>
         </ul>  
   </li>

    <% unless current_user.guest? %>
    <li><%= link_to raw("<i class='icon-large icon-off'></i> #{t('nav.logout')}"), destroy_user_session_path, :method=>'delete' %></li>
    <%else%>
    <li><%= link_to "Devenir Membre", new_member_path %></li>
  <%end%>
  <% else %>
  <li class = "dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%=raw("<i class='icon-large icon-play'></i> #{t('nav.connexion.title')}")%><b class="caret"></b></a>  
         <ul class="dropdown-menu">  
            <li><%= link_to t('nav.connexion.facebook'), user_omniauth_authorize_path(:facebook) %></li>
            <li><%= link_to t('nav.connexion.login'), new_user_session_path %></li>
            <li><%= link_to t('nav.connexion.signup'), new_user_registration_path %></li>
         </ul>  
  </li>
    
  <% end %>
  <!--
  <%= link_to "English", :locale=>'en'%>
  <%= link_to "Français", :locale=>'fr'%>
  -->
  
<%= simple_form_for([@user, @pesee]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :quand %>
    <%= f.input :what %>
    <%= f.input :user_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
